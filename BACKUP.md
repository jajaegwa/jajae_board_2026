# 데이터 백업 — 서버가 사라지거나 실수로 데이터가 지워졌을 때

자재과 작업판(`index.html`)·자재과 마감관리(`magam.html`)는 Supabase(외부 서버)에 실시간으로
저장됩니다. 이 문서는 그 서버 자체에 문제가 생기거나, 실수로 값을 지우거나 덮어썼을 때
되찾을 수 있는 방법을 정리한 것입니다.

## 지금 자동으로 되고 있는 것 (앱을 열 때마다 하루 1회, 아무것도 안 눌러도 됨)

실시간 공유가 연결된 상태로 관리 화면을 열면(기사 지게차 화면 제외), 다음 두 가지가
하루에 한 번 조용히 실행됩니다.

1. **이 기기에 백업 파일 자동 다운로드** — `자재과작업판백업-YYYY-MM-DD.json` /
   `마감백업-YYYY-MM-DD.json` 형태로 다운로드 폴더에 쌓입니다. **Supabase 서버 자체가
   완전히 사라져도** 이 파일들만 있으면 데이터를 되살릴 수 있습니다.
   - 일부 브라우저(특히 iOS Safari)는 사용자 동작 없이 시작된 다운로드를 막기 때문에,
     화면을 처음 조작하는 순간(다음 클릭·탭)에 맞춰 다운로드가 시작됩니다. 그래서 그날
     화면을 열기만 하고 아무 것도 누르지 않으면 그날은 건너뛸 수 있습니다 — 다음날 다시
     시도합니다.
   - 다운로드 폴더를 OneDrive·Google Drive 같은 클라우드 동기화 폴더로 지정해두면, 이
     자동 다운로드가 곧 오프사이트(서버 밖) 백업이 됩니다.
2. **Supabase에도 날짜 돌림칸(1~31일)으로 스냅샷 저장** — 그날이 이달의 며칠인지에 따라
   `jajae-main-backup-01` ~ `-31` (마감관리는 `jajae-magam-backup-01` ~ `-31`, 기준정보는
   `jajae-master-backup-01` ~ `-31`) 중 한 칸에 저장됩니다. 한 달이 지나면 같은 칸에 다시
   덮어써서 용량이 계속 늘어나지 않습니다. **서버는 살아있지만 실수로 값을 지우거나
   잘못 덮어썼을 때** 이 칸으로 되돌아갈 수 있습니다.

## 스냅샷에서 되살리는 방법

1. 작업판 상단 **삭제기록** 탭(또는 마감관리 **데이터·설정** 탭) → **서버 자동 백업** 카드.
2. 되돌리고 싶은 날짜(이달의 며칠, 1~31)를 입력하고 **그 날짜 서버 스냅샷 받기**.
3. 받아진 파일을 —
   - 작업판: **삭제기록 → 백업 파일 고르기**에 넣으면 **지금 없어진 항목만** 찾아
     되살립니다(기존 내용을 덮어쓰지 않음).
   - 마감관리: **데이터·설정 → JSON 가져오기**에 넣으면 현재 데이터를 그 스냅샷 내용으로
     통째로 바꿉니다(주의 — 그 사이 입력한 내용은 사라짐).

같은 방법으로 자동 다운로드된 로컬 파일(①)도 그대로 쓸 수 있습니다.

## 수동으로 지금 바로 백업하고 싶을 때

- 작업판 상단 **[백업]** 버튼 — 파일 다운로드 + 클립보드 복사를 한 번에.
- 마감관리 **데이터·설정 → JSON 백업 내려받기**.
- 마감관리 **데이터·설정 → 사급 CSV / 매입 CSV** — 엑셀로 열어볼 수 있는 형태.

## 더 강력한 보호가 필요하면 (Supabase 대시보드에서 한 번만 설정 — 관리자 본인만 가능)

위 방법들은 모두 **이 앱의 코드로 자동화한 것**이라 Supabase 계정 없이도 동작합니다.
다만 Supabase 프로젝트 자체가 삭제되거나(요금 미납 등) 계정 접근이 막히는 경우까지
막으려면, Supabase 대시보드에서 아래를 직접 확인·설정해야 합니다(이건 저장소 코드가
아니라 계정 소유자의 Supabase 설정이라 이 문서로 대신할 수 없습니다):

1. **Database → Backups** — Point-in-time Recovery(PITR)가 켜져 있는지 확인. 유료 플랜
   기능이며, 켜두면 최근 며칠~몇 주 안의 아무 시점으로나 통째로 되돌릴 수 있습니다.
2. **정기적으로 대시보드에서 수동 백업 다운로드**(무료 플랜이라면 특히) — Database →
   Backups에서 스냅샷을 내려받아 개인 드라이브 등 이 프로젝트와 무관한 곳에 보관.
3. **결제 수단·소유자 계정 정보를 최신으로 유지** — 요금 미납으로 인한 프로젝트 정지가
   가장 흔한 "서버가 사라지는" 원인입니다.

## 요약 — 무엇이 무엇을 막아주는가

| 위협 | 막아주는 방법 |
|---|---|
| 실수로 항목을 지우거나 잘못 덮어씀 | 삭제기록의 [되살리기], 또는 서버 스냅샷(①/②) |
| 이 기기(브라우저)의 저장 내용이 사라짐 | Supabase의 실시간 공유 데이터(다른 기기에 남아 있음) |
| Supabase 프로젝트 자체가 사라지거나 접근 불가 | 이 기기에 자동 다운로드된 백업 파일(①) — 클라우드 동기화 폴더에 두면 더 안전 |
| 그 자동 다운로드 파일마저 없음 | Supabase 대시보드의 Point-in-time Recovery / 수동 다운로드 백업(위 3번 항목) |
